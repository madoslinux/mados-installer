name: CI Debug

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Debug
        run: |
          echo "Debug run started"
          whoami
          python3 --version
          echo "Python OK"
          echo "Done"